bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation(project(":tf-core"))
    testImplementation(testFixtures(project(":tf-core")))

    implementation('org.springframework.boot:spring-boot-starter')

    runtime('com.h2database:h2:1.4.199')
}
